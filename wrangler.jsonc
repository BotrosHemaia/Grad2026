{
  "$schema": "node_modules/wrangler/config-schema.json",
  "name": "grad-party-seating",
  "compatibility_date": "2026-08-25",
  "pages_build_output_dir": "./dist"
  // This app is a static React SPA that talks directly to Firebase Firestore
  // from the browser. No Cloudflare bindings (D1/KV/R2) are needed — all
  // persistence lives in Firebase.
}
